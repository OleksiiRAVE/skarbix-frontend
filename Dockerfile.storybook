FROM node:22-alpine AS build

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build-storybook

FROM caddy:2-alpine

COPY Caddyfile.storybook /etc/caddy/Caddyfile
COPY --from=build /app/storybook-static /srv/storybook

EXPOSE 80
